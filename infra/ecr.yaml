AWSTemplateFormatVersion: '2010-09-09'

Resources:
  Repo:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: bedrock-agent

Outputs:
  RepoUri:
    Value: !GetAtt Repo.RepositoryUri
